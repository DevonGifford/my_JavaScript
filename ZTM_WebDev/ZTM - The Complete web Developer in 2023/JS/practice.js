const player = 'Devon';
let powerLevel =  90000;
let codeProgrammer = false;

if (powerLevel > 8999) {
    let codeProgrammer = true;
    console.log('inside', codeProgrammer);
		if (codeProgrammer = true) {
			let newLevel = true;
			console.log(newLevel)
		}
}

console.log('outside', codeProgrammer);
console.log(newLevel)